name: Deploy Frontend to Vercel
on:
  push:
    branches: [ main ]
jobs:
  vercel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Vercel CLI
        run: npm i -g vercel
      - name: Deploy
        run: vercel --prod --confirm
